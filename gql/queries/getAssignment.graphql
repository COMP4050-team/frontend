query getAssignment($id: ID!) {
  assignment(id: $id) {
    id
    classID
    unitID
    name
    tests {
      id
      name
    }
    submissions {
      id
      studentID
      result {
        id
      }
    }
  }
}
