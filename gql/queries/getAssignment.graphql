query getAssignment($id: ID!) {
  assignment(id: $id) {
    id
    class {
      id
      name
    }
    unit {
      id
      name
    }
    name
    tests {
      id
      name
    }
    submissions {
      id
      studentID
      result {
        id
      }
    }
  }
}
