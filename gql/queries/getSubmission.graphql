query getSubmission($id: ID!) {
  submission(id: $id) {
    id
    studentID
    unit {
      id
      name
    }
    class {
      id
      name
    }
    assignment {
      id
      name
    }
  }
}
