overwrite: true
schema: "http://localhost:8000/query"
documents: "gql/**/*.graphql"
generates:
  gql/generated/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typed-document-node"
